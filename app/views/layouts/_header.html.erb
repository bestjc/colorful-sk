<header class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <div>
      <a href="<%= root_path %>" class="navbar-brand">
        <%= image_tag("logo.png") %>
      </a>
      <ul class="nav navbar-nav navbar-right" style="margin-right:10px;">
        <li><%= link_to "首页",     root_url %></li>
        <li><%= link_to "帮助",     help_path %></li>
        <% if logged_in? %>
          <li><%= link_to "用户", users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              账户 <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "我的主页", current_user %></li>
              <li><%= link_to "设置", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "退出", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
          <li class="fx_btn">
            <button type"button" class="btn btn-success" data-toggle="modal" data-target="#postModal">分　享</button>
          </li>
        <% else %>
          <li><%= link_to "登录", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
